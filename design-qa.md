# CameraSimulator 1.0 设计与交互验收

日期：2026-09-05。选定方向：第 3 款，深绿桌面工作室。

## 验收来源和画面

- 视觉来源：`design/selected-concept.png`，1487 × 1058 px。
- 浏览器最终画面：`design/implementation-exposure-final.png`，1487 × 1058 px，CSS viewport 1487 × 1058，devicePixelRatio 1。
- 状态：相机定制 → 曝光，AURA / ONE，36 × 24 mm，50 mm，f/1.4–16，ISO 100–25600，1/8000–30 s；附属参数收起。
- 同一输入中的全屏对照：`design/comparison-full.png`，参考和实现左右并排，没有拉伸原图。工具显示时可能缩小合成图，细节判断使用下列局部图。
- 局部对照：`design/comparison-controls.png`、`design/comparison-model.png`。
- 桌面程序画面：`design/native-final.jpg`，1489 × 1033 px；Windows 可用屏幕限制了实际窗口高度。`design/comparison-native-controls.png` 仅去掉 1 px 窗口边框，按原始像素对照表单区域，没有拉伸。
- 内嵌浏览器的缩放显示会使捕获文字偏软，因此字体清晰度以原生桌面窗口的局部对照为准，不能把内嵌面板缩放造成的差异当作字体缺陷。

## 已修正的问题和复核历史

| 严重度 | 问题、证据和影响 | 修正 | 修正后证据 |
| --- | --- | --- | --- |
| P0 | 开发态 Canvas 双重挂载后黑屏，无法操作主要三维视图 | 去除开发 StrictMode 双重挂载；重新加载实际 GLB/HDR | 最终浏览器与原生截图完整显示模型 |
| P1 | 初期 681 个节点逐个提交导致三维视图额外开销 | 按材质合并静态网格，保留独立光学玻璃和可变铭文/光圈 | 旋转、缩放、前后顶部视角均实际操作；完整网格保留 |
| P2 | `implementation-exposure-before.png` 的上下堆叠标签使曝光区过高，辅助文字落到滚动区外 | 范围输入改为同排标签，收紧行距和页脚空白 | `implementation-exposure-after.png` 及最终 controls 对照 |
| P2 | 初期模型尺寸偏小、主视角遮住握柄 | 调整相机到左前上方，30° 视角；保持主画面由相机占据 | `implementation-exposure-final.png`、`comparison-model.png` |
| P2 | 初期蒙皮纹理过密，中文铭文被固定矩形压缩，镜头铭文太小 | 统一纹理比例、凹凸和粗糙度；按文字自然长宽比显示；镜头文字沿圆环排布 | `custom-brand.png` 与 `native-final.jpg` |
| P2 | 前拨轮朝向让正面看起来像黑孔，顶部刻度偏小 | 在 Blender 中重建拨轮方向并扩大刻度，重新导出 GLB 和可编辑源文件 | 最终模型与 `model-authoring-preview.png` |
| P0 | 加入第三方柔和阴影补丁时与当前 Three 着色器不兼容 | 移除补丁，使用 Three 自带 variance 阴影；新浏览器会话重新检查 | 最终新会话无 error；完整原生画面 |
| P2 | 精确参数编辑中输入框重挂载导致丢失焦点；JSON 快门显示格式会舍入任意数值 | 将 Field/Slider 组件移到稳定作用域；序列化保留原始秒数精度 | 连续中文/数字输入实际检查；任意快门精度回归测试 |
| P2 | 关闭窗口前的最后编辑可能来不及完成自动保存 | 原生关闭握手，写入最新草稿成功后再关闭 | Alt+F4 关闭与重启恢复实际验证 |

## 五项视觉检查

1. **字体与层级**：参考的中文衬线标题和英文衬线品牌由系统 SimSun / Georgia 对应；控件使用系统中文 UI 字体。主标题 29 px，英文品牌 24 px，标题、段落、参数区层级清楚。Native 对照确认没有截字、错行或输入值消失。参考图中的字体抗锯齿和模糊效果不复制成位图文字。
2. **布局与间距**：保留约 26.3% 左侧参数区、60 px 顶栏、右侧主三维区和三列底部规格。行内上下限、分组分隔线、四个分类和固定确认入口均保留。1100 × 720 的最小尺寸没有横向页面溢出，左侧内容可独立滚动；窗口按钮、主要确认按钮和三维控制保持可用。
3. **颜色与状态**：背景 `#0b211b`，左栏 `#10281f`，顶栏 `#061a13`，灰白与浅绿文字、薄分隔线、浅绿确认按钮。选中分类下划线、材质选中态、键盘焦点、必填错误和成功提示都可区分。
4. **模型与图像质量**：主图是可旋转的原创 Blender 网格，采用本地生成的细纹蒙皮、金属材质、HDR 环境、透射镜片、可变光圈叶片和阴影。没有把参考相机截图放在主屏充当模型。保留可编辑 `.blend`。参考图展示的是生成的产品摄影效果；本实现采用已选择的固定原创微单造型，肩部、握柄、拨轮和玻璃反射与参考中的相机不同。这一原创建模选择属于产品范围，并非逐像素复制参考资产；当前实时渲染不宣称达到与实拍照片难以区分的水平。
5. **文字与信息**：保留“定制你的相机”“从核心规格开始”“确定并补齐参数”等核心文案。新增常用范围、关键参数完成数、错误反馈、自动/手动来源和拍摄限制说明，是实际交互所需内容。实现细节、文件路径和工程边界放在帮助/README 中。

## 交互与构建验证

- 空白关键项阻止确认并聚焦首个缺失字段；示例必须由用户主动选择。传感器、焦距、ISO/快门/光圈范围均经过数值与上下限校验。
- 自动补齐产生确定的附属值，结果区区分自动和手动；手动设置保留，画幅变化后自动值重算。
- 中文品牌“山野”和型号“M-01”、三种配色、蒙皮/橡胶、铭文颜色实际操作；三维正面、背面、顶部、缩放、旋转、灯光均工作。
- 静物、夜景、运动场景及曝光变化实际检查：`simulation-still.png`、`simulation-night.png`、`simulation-motion-fast.png`、`simulation-motion-slow.png`、`simulation-underexposed.png`。快门 1/125 到 1/8000 的 6 EV 变化可见，直方图随之移动。
- JSON 保存并重新打开成功，样例 `design/AURA-ONE.camera.json`。PNG 通过 Windows 保存对话框导出，`design/sample-photo.png` 实际为 1600 × 1067 RGBA，照片中没有界面覆盖层。
- 精确输入支持分数快门和任意合法数值；参数受到自定义相机范围限制。视图宽高比跟随传感器。
- 键盘焦点轮廓、输入关联标签、错误说明、required 状态、弹窗 Tab 焦点约束以及 reduced-motion 已实现。此项为针对主流程的检查，不构成完整读屏认证。
- 最终 `npm run package` 内的类型检查、14 项参数/摄影计算测试、Vite 构建和 Electron 打包全部通过。完整输出在 `design/package-build.log`。
- 新的浏览器会话没有 console error。仍有依赖内部的 Three.Clock 弃用警告，不影响当前功能。Vite 提示主包大于 500 kB；本地桌面版本使用随包资源，无网络首次下载依赖。
- Windows EXE 成功启动、显示完整更新后的模型并恢复 AURA / ONE 草稿。包内三个主要模型/纹理/HDR 资产与源文件字节一致，包内 JS 与最终构建一致，版本 1.0.0。
- ZIP 的 79 项 CRC 检查通过；163365231 bytes。SHA-256 与结果保存在 `design/package-verification.json`。

## 预期边界与可继续打磨项

- 固定原创机身，品牌、配色、材质和光圈可变化；不根据任意参数自动生成可制造的机械结构。摄影模拟采用薄透镜、曝光公式及实时噪声/运动近似，具体范围与限制见 README。
- P3：可以继续增加金属表面微磨损、拨轮细刻字和更复杂的镜片镀膜层次，以接近参考图的产品摄影质感。
- 尚未覆盖低端集成显卡、多种 Windows 缩放比例和长时间压力测试。当前验证设备为 Windows 11 / RTX 3080 Ti Laptop。

## 实施检查清单

- [x] 同状态、同尺寸全屏对照和关键区域对照
- [x] 布局、字体、颜色、图像和文案检查
- [x] 主流程、最小窗口和原生文件操作
- [x] P0/P1/P2 问题修正及重新截图
- [x] 类型检查、14 项测试、最终打包
- [x] ZIP CRC 与包内资产一致性检查
- [x] 原生程序启动与草稿恢复

final result: passed
